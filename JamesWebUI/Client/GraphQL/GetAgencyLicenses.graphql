query GetAgencyLicenses($agencyId: UUID!) {
  agencyLicenses(agencyId: $agencyId) {
    id
    status
    state
    appointingState
    licenseNumber
    isResident
    insurerId
    insurer {
      id,
      idNavigation{
        fullName
      }
    }
  }
}
