query GetAgencyLicenses($agencyId: UUID!) {
  agencyLicenses(agencyId: $agencyId) {
    id,
    isActive,
    state,
    appointingState,
    licenseNumber,
    isResident,
    insurerId,
    insurer {
      id,
      idNavigation{
        fullName
      }
    }
  }
}
