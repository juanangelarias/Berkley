query SearchAgencies($searchString: String, $activeOnly: Boolean!) {
  searchAgencies(stringToSearch: $searchString, activeOnly: $activeOnly) {
      id
      agencyNumber
      agencyName
      city
      branch
      parentChild
      status
  }
}
