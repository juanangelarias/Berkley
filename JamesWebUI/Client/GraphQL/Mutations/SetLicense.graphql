mutation SetAgencyLicense ($input: SetLicenseInput!)
{
  setLicense(license: $input){
    agencyLicense{
      id,
      appointingState,
      appointment,
      comments,
      expiration,
      insurerId,
      isResident,
      licenseNumber,
      state,
      status,
      termination
    }
  }
}