@using James.Shared

@inject NavigationManager Navigation

<AuthorizeView>
    <Authorized>
        <RadzenProfileMenu Click="ProfileItemClicked" Style="background: #FCFCFC">
            <Template>
                <RadzenGravatar Email="TUser1@BerkleySurety.com" AlternateText="@context.User.Identity?.Name">
                </RadzenGravatar>
            </Template>
            <ChildContent>
                <RadzenProfileMenuItem Text="@("Hello, " + context.User.Identity.Name)" />
                <RadzenProfileMenuItem Text="My Profile" Path="profile" Icon="account_circle" />
                <RadzenProfileMenuItem Text="My Dashboard" Path="" Icon="home" />
                <RadzenProfileMenuItem Text="Logout" Icon="logout" />
            </ChildContent>
        </RadzenProfileMenu>
    </Authorized>
    <NotAuthorized>
        <a href="@JamesConstants.LOG_IN_PATH?redirectUri=@Navigation.Page()">Log in</a>
    </NotAuthorized>
</AuthorizeView>

@code {
    private async Task ProfileItemClicked(RadzenProfileMenuItem item)
    {
        if (item.Text == "Logout")
            //NOTE:  forceLoad must be true below to prevent the client side from trying to handle the logout request
            Navigation.NavigateTo(JamesConstants.LOG_OUT_PATH + "?redirectUri=/", true);
    }

}
