query GetAllUserSettings {
    allUserSettings {
        key
        value
    }
}

query GetUserEmployeeInfo {
    userEmployeeInfo {
        email
        employeeId
        fullName
        isUnderwriter
        title
        username
        homeOfficeApprover
    }
}

query GetUserLOAByDivision($division: String!) {
    userLOAByDivision(division: $division) {
        id
        divisionCode
        bondType
        expiration
        single
        aggregate
    }
}