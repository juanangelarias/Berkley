@rendermode InteractiveWebAssembly
@page "/Agency"

@using System.Globalization
@using JamesWebUI.Client.GraphQL;
@using JamesWebUI.Client.GraphQL.Components;


@inherits LayoutComponentBase

@inject NavigationManager NavigationManager
@inject ThemeService ThemeService
@inject DialogService DialogService
@inject AccountClient AccountClient
@inject ContextMenuService ContextMenuService

<Theme />


<PageTitle>Agency Profile</PageTitle>

<RadzenLayout>
    <RadzenHeader>

    </RadzenHeader>
    <RadzenBody>
        <RadzenRow Gap="1" RowGap="1" JustifyContent="JustifyContent.Right">
            <RadzenColumn Style="vertical-align: middle" Size="1">
                <RadzenLabel Text="Search by:" Component="txtSearch"  ></RadzenLabel>
            </RadzenColumn>
            <RadzenColumn Size="3">
                <RadzenTextBox Placeholder="Agency/Producer No." Name="txtSearch" Style="width: 100%"/>
            </RadzenColumn>
            <RadzenColumn Size="4">
                <RadzenTextBox Placeholder="Name" Style="width: 100%" />
            </RadzenColumn>
            <RadzenColumn Style="vertical-align: middle">
                <RadzenButton Icon="search" Size="ButtonSize.Small" />
            </RadzenColumn>
            <RadzenColumn>
                <RadzenButton Variant="Variant.Filled" ButtonStyle="ButtonStyle.Primary" Text="Create New Agency" />
            </RadzenColumn>
        </RadzenRow>
        <RadzenRow></RadzenRow>
        <RadzenRow RowGap="1" >
            <RadzenColumn Size="1"/>
            <RadzenStack Orientation="Orientation.Horizontal">
@*                 <UseGetAllStates Context="state">
                    <ChildContent>
                        <RadzenDropDown @rendermode="@InteractiveWebAssembly" Placeholder="States" TValue="GetAllStates_AllStates_State" TextProperty="state.Code" ValueProperty="state.Code" id="stateDropDown">
                            
                        </RadzenDropDown>
                    </ChildContent>
                    <LoadingContent>
                        <RadzenDropDown @rendermode="@InteractiveWebAssembly" Placeholder="Loading..." TValue="GetAllStates_AllStates_State"/>
                    </LoadingContent>
                </UseGetAllStates> *@
                <RadzenDropDown @rendermode="@InteractiveWebAssembly" TValue="string">
                    <RadzenDropDownItem TValue="string">Test1</RadzenDropDownItem>    
                    <RadzenDropDownItem TValue="string">Test2</RadzenDropDownItem>
                    <RadzenDropDownItem TValue="string">Test3</RadzenDropDownItem>
                </RadzenDropDown>
                <RadzenDropDown id="testDropDown" @rendermode="@InteractiveWebAssembly" Placeholder="Branch" TValue="string" />
                <RadzenDropDown @rendermode="@InteractiveWebAssembly" Placeholder="Status" TValue="string" />
            </RadzenStack>
        </RadzenRow>
    </RadzenBody>
</RadzenLayout>


@code {
    private IGetAllStatesResult states;

    protected override void OnInitialized()
    {
        ThemeService.Initialize(NavigationManager);
    }
}

